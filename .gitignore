# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
functions/node_modules/
.idea/
.vscode/

# Android
android/.gradle/
android/local.properties
android/key.properties
android/app/release/
*.jks
*.keystore

# Secrets — DO NOT COMMIT
android/app/google-services.json
lib/core/constants/admob_ids.dart
# api_constants.dart no longer holds real tokens — it points at the Cloudflare
# Worker proxy, so it is safe to commit. Removed from gitignore.
remoteconfig.template.json
# Worker local secrets (never commit .dev.vars)
worker/.dev.vars

# Firebase Functions proxy (replaced by Cloudflare Worker)
functions/

# OS
.DS_Store
